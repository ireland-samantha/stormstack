# Lightning Engine - Docker Compose Environment Variables
# Copy this file to .env and customize as needed

# =============================================================================
# Authentication Configuration
# =============================================================================

# JWT secret for authentication (MUST change in production)
# Used by auth service for token signing, and by other services for validation
AUTH_JWT_SECRET=change-me-in-production-use-long-random-string

# Initial admin password (change on first login)
ADMIN_INITIAL_PASSWORD=admin

# =============================================================================
# Control Plane Configuration
# =============================================================================

# Token for control plane API access (service-to-service authentication)
CONTROL_PLANE_TOKEN=dev-token

# =============================================================================
# Docker Image Overrides (optional)
# =============================================================================

# Default: Docker Hub public images
# For local development with Tailscale registry, set to:
#   AUTH_IMAGE=$TAILSCALE_IP:5001/lightning-auth:0.0.3-SNAPSHOT
#   CONTROL_PLANE_IMAGE=$TAILSCALE_IP:5001/lightning-control-plane:0.0.3-SNAPSHOT
#   ENGINE_IMAGE=$TAILSCALE_IP:5001/lightning-engine:0.0.3-SNAPSHOT

# AUTH_IMAGE=samanthacireland/lightning-auth:0.0.3-SNAPSHOT
# CONTROL_PLANE_IMAGE=samanthacireland/lightning-control-plane:0.0.3-SNAPSHOT
# ENGINE_IMAGE=samanthacireland/lightning-engine:0.0.3-SNAPSHOT

# =============================================================================
# Advanced Configuration (usually not needed)
# =============================================================================

# MongoDB connection (default is fine for local development)
# QUARKUS_MONGODB_CONNECTION_STRING=mongodb://mongodb:27017

# CORS origins for production (uncomment and set your domain)
# CORS_ORIGINS=https://yourdomain.com
