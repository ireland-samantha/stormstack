<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.lightningfirefly</groupId>
        <artifactId>backend</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>gui-acceptance-test</artifactId>
    <name>GUI Acceptance Tests</name>
    <description>Business use case acceptance tests using GUI with OpenGL and Testcontainers backend</description>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Skip tests by default since these require Docker + OpenGL display -->
        <skipTests>true</skipTests>
    </properties>

    <dependencies>
        <!-- GUI Module -->
        <dependency>
            <groupId>com.lightningfirefly</groupId>
            <artifactId>engine-gui</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Game Renderer Module -->
        <dependency>
            <groupId>com.lightningfirefly</groupId>
            <artifactId>game-renderer</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Game API Module -->
        <dependency>
            <groupId>com.lightningfirefly</groupId>
            <artifactId>engine-adapter</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Rendering Test Utilities -->
        <dependency>
            <groupId>com.lightningfirefly</groupId>
            <artifactId>engine-rendering-test</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- JSON Parsing -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.3</version>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers (version from parent BOM) -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- SLF4J for logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>2.0.16</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <!-- Surefire for unit tests (also runs IT tests if requested) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- Required for LWJGL/GLFW on macOS -->
                    <argLine>-XstartOnFirstThread --enable-native-access=ALL-UNNAMED</argLine>
                    <systemPropertyVariables>
                        <enableGLTests>true</enableGLTests>
                    </systemPropertyVariables>
                    <forkedProcessExitTimeoutInSeconds>180</forkedProcessExitTimeoutInSeconds>
                    <forkedProcessTimeoutInSeconds>300</forkedProcessTimeoutInSeconds>
                </configuration>
            </plugin>
            <!-- Failsafe for integration tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- Required for LWJGL/GLFW on macOS -->
                    <argLine>-XstartOnFirstThread --enable-native-access=ALL-UNNAMED</argLine>
                    <systemPropertyVariables>
                        <enableGLTests>true</enableGLTests>
                    </systemPropertyVariables>
                    <forkedProcessExitTimeoutInSeconds>180</forkedProcessExitTimeoutInSeconds>
                    <forkedProcessTimeoutInSeconds>300</forkedProcessTimeoutInSeconds>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Profile to run GUI acceptance tests -->
        <profile>
            <id>acceptance-tests</id>
            <properties>
                <skipTests>false</skipTests>
            </properties>
        </profile>
    </profiles>
</project>
